"""Model Evaluation (Section 6.5) — for every trained model, runs a
rolling/expanding backtest, computes accuracy metrics, generates the
forward forecast, and applies forward-validation elimination rules before
a model can reach Ranking.
"""
