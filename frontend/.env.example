# The only frontend configuration there is.
#
# Entra ID settings are deliberately NOT here: the app fetches them from the
# backend's /auth/config at runtime, so one built bundle can be deployed to
# any environment. No client id, tenant, secret or API key is ever baked
# into the frontend build.
VITE_API_BASE_URL=http://localhost:8000
