fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9
pandas==2.2.2
openpyxl==3.1.5
# Client-only MLflow: the backend reads completed-run history from the
# tracking store but never trains or serves a model, so the full mlflow
# distribution (server, UI, ML frameworks) is not needed. Bundles the
# Databricks SDK, which is what lets the same reader work against a
# Databricks tracking server with only the URI changing.
mlflow-skinny==2.17.2
# mlflow-skinny's MlflowClient needs this to talk to a local sqlite tracking
# store (the local-dev default and what tests run against) — mlflow-skinny
# itself does not bundle it. Was already present transitively in every
# developer's venv but missing from this file, so a clean install (CI) fails.
sqlalchemy>=2.0,<3
alembic>=1.13,<2

# Dataset preview: reads the uploaded file back from ADLS for the Results
# page. Read-only — the backend is given a container-scoped SAS, never an
# account key.
azure-storage-blob>=12.19,<13

# Entra ID token validation (app/auth/entra.py) — RS256 signature
# verification against the tenant's public JWKS via PyJWKClient, hence the
# `crypto` extra (pulls in `cryptography`). Was already installed in every
# developer's venv but missing from this file — nothing new is added to
# auth itself, this only makes the existing dependency reproducible.
PyJWT[crypto]==2.13.0

# Test-only: fastapi.testclient.TestClient (Starlette) requires httpx to be
# installed to instantiate at all, even though no test imports it directly.
httpx==0.28.1

# Production WSGI process manager for Azure App Service (Linux). uvicorn
# alone is fine for local `uvicorn app.main:app --reload`; App Service's
# startup command runs gunicorn with uvicorn's own worker class instead, for
# proper multi-worker process management and signal handling under a real
# process supervisor.
gunicorn==23.0.0
